# Python
__pycache__/
*.py[cod]
*.pyo
*.pyc
*.so
*.egg-info/
.eggs/
*.egg
.pytest_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/
ENV/

# Build & packaging
build/
dist/
*.whl

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor & IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cache/
.zed

# Logs & artifacts
*.log
ci.log
stage/
*.deb
*.rpm

# Local git worktrees (the /do workflow puts them here). Never part of a
# commit — they are separate checkouts, so `git add -A` from the repo root
# would otherwise stage them as embedded git repositories.
.worktrees/
